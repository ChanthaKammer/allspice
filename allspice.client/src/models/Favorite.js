export default class Favorite {
   constructor(data){
      this.id = data.id;
      this.accountId = data.accountId;
      this.recipeId = data.recipeId;
   }
}

//You might need to add your favorite recipe as a class here, for now I am excluding it.

